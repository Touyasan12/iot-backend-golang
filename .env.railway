# Railway Environment Variables
# Copy these to Railway Dashboard: Project Settings > Variables

# Server Configuration
SERVER_PORT=8080

# Database Configuration (PostgreSQL on Railway)
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
DB_NAME=${{Postgres.PGDATABASE}}
DB_TYPE=postgres

# MQTT Configuration (HiveMQ Cloud)
MQTT_BROKER=5aed062215ab4b2f950052625f538fef.s1.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USER=ghalytsarhivemq
MQTT_PASS=Ember1233
MQTT_CLIENT_ID=aquarium-backend

# Mode Configuration
# Set to "false" for production (real MQTT connection)
# Set to "true" for testing without MQTT hardware
DEMO_MODE=false
